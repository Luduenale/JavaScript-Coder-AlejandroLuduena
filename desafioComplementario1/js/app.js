/*
//        DO...WHILE

let numeroIngresado;
let suma = 0;
do {
  numeroIngresado = Number(
    prompt("Ingrese un numero, ingrese 0 para terminar")
  );
  suma = suma + numeroIngresado;
  console.log(suma);
} while (numeroIngresado != 0);
*/

/*
//          WHILE

let entradaConcatenada = '';
let entrada = prompt("Ingrese un texto o una palabra, ESC para finalizar");

while (entrada != "ESC" && entrada != "esc") {
  entradaConcatenada = entradaConcatenada + entrada + " ";
  console.log(entradaConcatenada)
entrada = prompt("Ingrese otro texto u otra palabra, ESC para finalizar");
}
*/

/*
//           FOR
let numeroIngresado = Number(prompt('Ingresa un numero'))
for(let i = 1; i <= numeroIngresado ; i++){

    console.log('Hola')

}
*/
